{
  "name": "glosa",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "packages/providers/*", "packages/adapters/*"],
  "engines": { "bun": ">=1.2.7" },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  }
}
